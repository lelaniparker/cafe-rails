<h1>Welcome to Coder Cafe!</h1>

<table>
 <tr>
   <th>Item</th>
   <th>Price</th>
 </tr>
 <% @menu.each do |item| %>
 <tr>
   <td><%= item.name %></td>
   <td><%= "$%.2f" % item.price %></td>
   <td><%= link_to 'Order', new_order_item_path(:item_name => item.name) %></td>
 </tr>
 <% end %>
</table>